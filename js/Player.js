/* class Player extends Sprite {
    constructor({
        position,
		velocity,
		image,
		frames = { max: 1, hold: 10 },
		sprites,
		animate = false,
		rotation = 0,
		scale = 1,
		inventory
    }) {
		this.inventory = {
			chicken: false,

		}
	}
} */